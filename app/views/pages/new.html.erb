<% if current_user %>

  <% posts = current_user.tumblr_client.posts("#{current_user.uid}.tumblr.com")['posts'] %>
  
  <% posts.each do |post| %>
    <%= button_to post['title'], pages_path(@page, original_url: post['post_url']) %><br>
  <% end %>

<% end %>